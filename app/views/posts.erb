<br>
<div class="ui relaxed divided list">
  <% @posts.each do |post| %>
    <div class="item">
      <i class="large bookmark middle aligned icon"></i>
      <div class="content">
        <a class="header"><%= post.created_at %></a>
      </div>
    </div>
  <% end %>
</div>
